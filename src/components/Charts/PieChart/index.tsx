// import { PieChart as RechartsPieChart } from 'recharts';

type PieChartProps = {
  xKey: string;
  yKey: string;
};

export default function PieChart({ xKey, yKey }: PieChartProps): JSX.Element {
  return <></>;
}
